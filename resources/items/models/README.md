# Item Models

这里放物品级别的 3D 展示模型，主要用于：

- 3D 柜子里的物品展示；
- 玩家拾取/掉落物；
- 外出携带物的预览。

完整柜子、家具、房间 prop 不放这里，应放 `levels/props/`。

## 子目录

- `kenney/`：Kenney 来源的低模物品。
- `energy/`：能源类物品模型，如燃料罐、电力芯。
- `legacy/`：旧场景里仍使用的物品展示模型；后续可逐步替换为正式 `kenney/` 或自制模型。
